// Represents one contact entity. This will be the profile column that displays when the contact entity is clicked on in the contacts list.

import React from 'react'

const ContactProfile = () => {
  return (
    <div>ContactProfile</div>
  )
}

export default ContactProfile