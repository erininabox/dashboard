// Ingest metadata for single message, display here
// Import ReadIndicator.jsx to use, determine if message is read, then send in status as prop to component.

import React from 'react'

const MessageSingle = () => {
  return (
    <div>MessageSingle</div>
  )
}

export default MessageSingle