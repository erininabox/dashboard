// Imports MessageSingle.jsx as component. Messages data for a thread is then mapped through it to display individual messages in a thread.

// Important logic:
    // - If from self, show message on the left, otherwise message on the right
    // - Indicate which messages are unread by self
    // - Bottom of thread component shows SendMessageForm

import React from 'react'
import PropTypes from 'prop-types'

const MessageThread = props => {
  return (
    <div>MessageThread</div>
  )
}

MessageThread.propTypes = {}

export default MessageThread